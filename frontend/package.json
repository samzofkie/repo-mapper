{
  "name": "repo-mapper-frontend",
  "version": "1.0.0",
  "description": "The frontend to a tool for visualizing code repositories.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite --host",
    "build": "vite build"
  },
  "author": "Sam Zofkie",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "nanoid": "^5.0.8"
  }
}
